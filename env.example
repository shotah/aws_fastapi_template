# API Client Configuration
# Copy this file to .env and customize for your environment

# =============================================================================
# API Endpoint Configuration
# =============================================================================
# Full API Gateway URL (required)
API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/Dev/hello

# HTTP Method (default: GET)
API_METHOD=GET

# Request body as JSON string (only for POST/PUT/PATCH)
API_DATA=

# Request timeout in seconds (default: 30)
API_TIMEOUT=30

# =============================================================================
# Examples for Different Endpoints
# =============================================================================

# Example 1: GET /health
# API_URL=https://abc123.execute-api.us-east-1.amazonaws.com/Dev/health
# API_METHOD=GET

# Example 2: POST /users (create user)
# API_URL=https://abc123.execute-api.us-east-1.amazonaws.com/Dev/users
# API_METHOD=POST
# API_DATA={"name":"John Doe","email":"john@example.com","age":30}

# Example 3: POST /files (upload file)
# API_URL=https://abc123.execute-api.us-east-1.amazonaws.com/Dev/files
# API_METHOD=POST
# API_DATA={"file_name":"test.txt","content":"SGVsbG8gV29ybGQh","content_type":"text/plain"}

# =============================================================================
# AWS Credentials (automatically detected)
# =============================================================================
# The script automatically uses AWS credentials from:
#   1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
#   2. AWS CLI profile (set AWS_PROFILE below or in environment)
#   3. IAM role (if running on EC2/Lambda/ECS)

# Optional: Specify AWS profile if not using default
# AWS_PROFILE=dev

# See scripts/README.md for full documentation
